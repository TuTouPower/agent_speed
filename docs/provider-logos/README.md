# Provider logos（从单价仓同步）

来源：`FeiZhuLulu/real-api-pricing` 的 `web/src/assets/provider-logos/`（厂商级图标，不是每个模型单独一图）。

模型 → 文件映射见上一级 [`provider-logo-map.json`](../provider-logo-map.json)。

站仓请把本目录文件拷到自己的 `logos/`（或按 map 选用），并按 `model_to_logo[model].file` 解析图标。
