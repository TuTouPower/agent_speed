# Local secrets for agent_rank (never commit real .env).
# Benchmark harnesses may need provider API keys in the environment or their own CLIs.
# Public board deploy credentials live in external deployment environment (not this repo).
