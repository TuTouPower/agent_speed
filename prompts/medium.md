用中文回答，纯文本输出（代码放在 markdown 代码块里）。

任务：写一个 Python 模块，实现线程安全的 LRU 缓存，要求：

1. 支持 get / put / 容量上限与 LRU 淘汰；
2. 支持按 key 的 TTL 过期；
3. 线程安全；
4. 总长（含注释与用法示例）150～250 行。

要求：

- 直接输出答案正文，不要调用任何工具，不要读写文件，不要执行命令。
- 不要输出思考过程，只要最终答案（简短说明 + 完整代码 + 用法示例）。
